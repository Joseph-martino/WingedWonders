import { ContinentPipeColourPipe } from './continent-pipe-colour.pipe';

describe('ContinentPipeColourPipe', () => {
  it('create an instance', () => {
    const pipe = new ContinentPipeColourPipe();
    expect(pipe).toBeTruthy();
  });
});
